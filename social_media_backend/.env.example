# PUBLIC_INTERFACE
# Comma-separated list of allowed origins for CORS. For local React dev server:
CORS_ALLOW_ORIGINS=http://localhost:3000

# SQLite database file path. Relative paths are resolved from the backend project root.
# Default used by the app if not set: data/social_media.db
SQLITE_DB=./data/social_media.db

# Optional session TTL in minutes (if session features are used)
SESSION_TTL_MINUTES=120
